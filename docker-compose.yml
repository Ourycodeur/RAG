version: "3.9"

services:
  api_rag:
    build:
      context: .
      dockerfile: Dockerfile

    container_name: api_rag

    ports:
      - "8000:8000"

    env_file:
      - .env

    restart: unless-stopped

    volumes:
      - ./Pre-Processing/data:/app/Pre-Processing/data